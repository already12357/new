window.onload = function(e) {
    load_Native1('native-style-1');
}

function load_Native1(comJqSelector) {
    // init native form
    init_Native1(comJqSelector);

    // bind form event
    bindEvent_Native1(comJqSelector);
}